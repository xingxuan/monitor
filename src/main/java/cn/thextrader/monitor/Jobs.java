package cn.thextrader.monitor;

public class Jobs {
/*
    protected static Map cachedMap = new HashMap();
    static Random random = new Random();

    public Jobs() {
        System.out.println("jobs...............");
    }

    public void run() {
        long t = System.currentTimeMillis();
        cachedMap.put("time", t);

        List<ThreadInf> listMap = new ArrayList<ThreadInf>();
        for (int i = 0; i < 50; i++) {
            int state = random.nextInt(4) + 1;
            ThreadInf object = new ThreadInf();
            object.setState(state);
            switch (state) {
                case 1:
                    object.setS1(object.getS1() + 1);
                    break;
                case 2:
                    object.setS2(object.getS2() + 1);
                    break;
                case 3:
                    object.setS3(object.getS3() + 1);
                    break;
                case 4:
                    object.setS4(object.getS4() + 1);
                    break;
                default:
                    System.out.println(state + "thread state is ignored");
            }
            object.setName("Inactive RequestProcessor thread [Was:Default RequestProcessor/com.sun.tools.visualvm.tools.jmx.CachedMBeanServerConnectionFactory$SnapshotInvocationHandler$" + i + "]");
            object.setId(189 + i * 2);
            listMap.add(object);
        }
        cachedMap.put("list", listMap);
    }
*/


}
